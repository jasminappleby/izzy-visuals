# izzy-visuals

Run `npm install` on both master dir and then in /my-app, if they are not installed.

To run the backend of this website (contact form), run `node server.js`

To run the frontend of this website, navigate to /my-app and run `npm run dev` 